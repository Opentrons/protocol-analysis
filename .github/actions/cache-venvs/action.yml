name: Cache Analysis Venvs
description: Restore the analysis virtualenvs under .venvs/

runs:
  using: composite
  steps:
    - name: Restore .venvs
      uses: actions/cache/restore@v4
      with:
        path: |
          .venvs
        key: >-
          venvs-${{ runner.os }}-py3.12-${{ hashFiles('uv.lock', 'evaluate/env_config.py') }}
        restore-keys: |
          venvs-${{ runner.os }}-py3.12-${{ hashFiles('uv.lock', 'evaluate/env_config.py') }}-
          venvs-${{ runner.os }}-py3.12-
